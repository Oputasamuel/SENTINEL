# Keep the Google AI Studio project on its free tier. BugMind cannot inspect billing.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
# Explicitly acknowledge the project uses the free tier before live calls.
GEMINI_FREE_TIER_CONFIRMED=false
BUGMIND_DAILY_REQUEST_LIMIT=12
