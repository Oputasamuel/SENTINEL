# Demo script

Sign in to the dashboard and follow its terminal tutorial. Configure your own provider key through the CLI hidden prompt, list live models, and select one. Initialize a Solidity project and review a file. Confirm a supported finding with a reason. Start a new terminal, review another contract, and show the recalled cross-file invariant. Show the account's synchronized review summaries, then revoke CLI sessions to demonstrate access control.

For a baseline, repeat a review with --without-memory. Show actual outputs; do not promise different results or complete security coverage. Explain that Sibyl is connected alongside the pinned Pashov API adaptation; ACP and Base remain planned. Show the run metadata and actual specialist outputs. State the 14-call provider cost before running.
